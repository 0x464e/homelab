tcp:
  routers:
    apex:
      rule: "HostSNI(`{{ domain }}`)"
      entryPoints: ["lan-https"]
      service: apex
      tls:
        passthrough: true

  services:
    apex:
      loadBalancer:
        servers:
          - address: "{{ domain }}:443"
